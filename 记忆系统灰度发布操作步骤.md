# 天之灵记忆系统修复 — 灰度发布操作步骤

> 适用版本：第一轮（P0/P1/三级证据注入/P2）+ 第二轮（批量录入 bug 修复/LLM 增强正则替换）
> 发布日期：____年__月__日
> 执行人：________
> 回滚负责人：________

---

## 一、灰度策略总览

当前架构为 docker-compose 单集群，无现成蓝绿/金丝雀机制。灰度按**服务角色**分三阶段推进，风险从低到高：

| 阶段 | 服务 | 风险等级 | 核心变化 | 观察期 |
|---|---|---|---|---|
| 第一阶段 | `tzl_memory_worker` | 🟡 中 | 批量录入 bug 修复（10条全处理）+ 批量 LLM 抽取 + LLM 增强正则 | 24-48 小时 |
| 第二阶段 | `tzl_node` | 🔴 高 | candidate 弱注入（【待确认】记忆进入上下文）+ 证据匹配升级 | 48-72 小时 |
| 第三阶段 | `tzl_admin_node` + `tzl_admin_web` + `tzl_nginx` | 🟢 低 | 管理后台记忆列表接口 | 即时 |

**为什么先发布 memory_worker**：它是后台异步任务，不直接影响用户聊天体验。即使批量 LLM 抽取出问题，最多是记忆写入异常，不会导致用户聊天中断。先让它跑稳定，确认记忆写入量和质量符合预期，再发布用户面。

**关键约束**：
- 三个阶段**不要在同一天完成**，每阶段必须观察足够时间
- 发布时间选在**用户低峰期**（建议凌晨 1:00-5:00，或工作日上午 10:00 前）
- 发布前确认**回滚镜像已就绪**（发布脚本自动备份）
- **不要**在发布时同时打开 `NODE_MILVUS_RETRIEVAL_MODE=active` 和 `NODE_CHAT_TOOLS_MODE=active`——这两个开关单独评估，等代码稳定运行 1 周后再考虑

---

## 二、发布前检查清单（所有阶段共用）

### 2.1 代码与构建

- [ ] 本地测试通过：`cd apps/node && NODE_ENV=unittest npx jest --runInBand test/service/agent-profile-fact.service.test.ts test/service/user-identity-memory.service.test.ts test/service/person-temporal-memory.test.ts test/service/agent-evidence.test.ts`（预期 138 项全过）
- [ ] TypeScript 编译通过：`cd apps/node && npx tsc --noEmit`（预期零错误）
- [ ] 实体 dist 已重建：`cd packages/entities && npm run build`（firstSourceMessageId/latestSourceMessageId 字段已在 dist 中）
- [ ] 代码已提交到生产分支，commit hash 记录：`________`
- [ ] 发布脚本 plan 预览：`cd /opt/tianzhiling && bash scripts/release-production.sh plan <branch> <commit>`，确认将发布的服务列表符合预期

### 2.2 生产环境状态

- [ ] 所有服务健康：`curl -s https://tianzhiling.chat/api/system/health` 返回 `status: ok`
- [ ] 管理后台健康：`curl -s https://admin.tianzhiling.chat/admin_api/system/health` 返回 `status: ok`
- [ ] 容器无频繁重启：`docker ps --format '{{.Names}} {{.Status}}' | grep tzl_`，确认 RestartCount 为 0 或稳定
- [ ] MongoDB 连接正常：`docker exec tzl_mongo mongosh --eval "db.adminCommand('ping')"`
- [ ] Milvus 健康：`curl -s http://127.0.0.1:19091/healthz`
- [ ] Redis 连接正常：`docker exec tzl_redis redis-cli ping` 返回 PONG
- [ ] 磁盘空间充足：`df -h /opt/tianzhiling`，剩余 > 20%
- [ ] 记忆管道无大量积压：查看 `memory_pipeline_task` 集合 pending/failed 计数，记录基线值：pending=____ failed=____

### 2.3 数据基线（发布前记录，用于发布后对比）

- [ ] `agent_profile_fact` 集合总记录数：`________`
- [ ] `agent_profile_fact` 中 status=active 数量：`________`
- [ ] `agent_profile_fact` 中 status=candidate 数量：`________`
- [ ] 最近 24 小时新增记忆事实数：`________`
- [ ] 最近 24 小时 `memory_pipeline_task` 完成数：`________`
- [ ] 最近 24 小时 LLM 记忆抽取调用量：`________`
- [ ] 最近 24 小时 LLM 记忆抽取失败率：`________`

> 记录方式：在 MongoDB 中执行对应 count 查询，或通过管理后台 `GET /admin_api/memories` 接口统计。

### 2.4 回滚准备

- [ ] 确认当前运行版本 commit hash：`________`（回滚目标）
- [ ] 确认回滚脚本可用：`bash scripts/rollback-production.sh plan <当前commit>` 能正常输出计划
- [ ] 确认回滚镜像存在：`docker images | grep rollback`
- [ ] 回滚操作人已就位，知道回滚命令：`bash scripts/rollback-production.sh execute <当前commit>`

---

## 三、第一阶段：发布 tzl_memory_worker（后台记忆处理）

### 3.1 操作步骤

> 注意：发布脚本 `release-production.sh` 会自动检测变更并发布所有相关服务。为了只发布 memory_worker，我们采用"先构建镜像，再手动单服务启动"的方式。

**步骤 1：构建镜像（不启动）**

```bash
cd /opt/tianzhiling

# 拉取最新代码
git fetch origin
git checkout <branch>
git pull

# 预览发布范围
bash scripts/release-production.sh plan <branch> <commit>
# 预期输出包含：tzl_node, tzl_memory_worker, tzl_admin_node（因为 packages/entities 和 apps/node 都有变更）

# 执行发布构建（脚本会构建所有变更服务的镜像，但只启动后端服务）
# 注意：这一步会启动所有变更的后端服务！如果只想先启动 memory_worker，需要在脚本启动后手动停掉 tzl_node
# 更安全的方式：先备份当前 tzl_node 镜像，然后执行发布，发布后立即将 tzl_node 回滚到旧镜像

# 方案 A（推荐）：完整发布后立即回滚 tzl_node
bash scripts/release-production.sh <branch> <commit>

# 发布完成后，立即将 tzl_node 回滚到旧版本（只保留 memory_worker 为新版本）
docker image tag <旧tzl_node镜像ID> tianzhiling-tzl_node:latest
docker compose --profile prod up -d --no-deps --force-recreate tzl_node

# 确认 tzl_node 已回滚
docker inspect -f '{{.Config.Image}}' tzl_node
# 确认 tzl_memory_worker 为新版本
docker inspect -f '{{.Config.Image}}' tzl_memory_worker
```

**方案 B（更精细，需手动构建）**：

```bash
cd /opt/tianzhiling

# 只构建 memory_worker 镜像（复用 tzl_node 的 Dockerfile，因为 memory_worker 用相同镜像）
docker build -t tianzhiling-tzl_node:memory-fix \
  --build-arg RELEASE_VERSION=memory-fix \
  -f apps/node/Dockerfile .

# 标记为 memory_worker 使用的镜像
docker image tag tianzhiling-tzl_node:memory-fix tianzhiling-tzl_node:latest

# 只重启 memory_worker
docker compose --profile prod up -d --no-deps --force-recreate tzl_memory_worker

# 确认 tzl_node 未被影响
docker inspect -f '{{.Config.Image}}' tzl_node
```

> 建议用方案 A，因为发布脚本会自动处理镜像备份、健康检查、稳定性等待，更安全。发布后立即回滚 tzl_node 的操作要在 1 分钟内完成。

**步骤 2：验证服务状态**

```bash
# 确认 memory_worker 容器运行正常，无重启
docker ps --format '{{.Names}} {{.Status}}' | grep memory_worker

# 查看 memory_worker 启动日志，确认无报错
docker logs tzl_memory_worker --tail 100

# 确认 memory_worker 已连接到 MongoDB 和 Redis
docker logs tzl_memory_worker 2>&1 | grep -E 'mongo|redis|connected|error' | tail -20
```

**步骤 3：验证记忆管道开始处理**

```bash
# 等待 5-10 分钟，让 memory_worker 开始处理积压的批量任务
sleep 300

# 查看 memory_pipeline_task 处理情况
# pending 应该开始下降，completed 应该开始上升
docker exec tzl_mongo mongosh --eval "
db.getSiblingDB('tzl').memory_pipeline_task.aggregate([
  {\$group: {_id: '\$status', count: {\$sum: 1}}}
])"

# 查看最近 10 分钟新增的 agent_profile_fact 记录
docker exec tzl_mongo mongosh --eval "
db.getSiblingDB('tzl').agent_profile_fact.countDocuments({
  createdAt: {\$gte: new Date(Date.now() - 600000)}
})"
```

### 3.2 第一阶段观察指标（24-48 小时）

| 指标 | 基线值 | 预期变化 | 告警阈值 | 查看方式 |
|---|---|---|---|---|
| 记忆写入量（每日新增 agent_profile_fact） | ____ | 上升约 5-10 倍（之前 10 条只处理 1 条） | < 基线 2 倍（说明批量修复未生效） | MongoDB count / admin 接口 |
| memory_pipeline_task 完成率 | ____ | 稳定或上升 | < 80% | MongoDB aggregate |
| memory_pipeline_task failed 数 | ____ | 接近 0 | > 每日 10 条 | MongoDB count |
| LLM 记忆抽取调用量 | ____ | 下降约 50-80%（批量 1 次调用替代 10 次单条） | > 基线（说明批量抽取未生效） | LLM 平台账单 / 应用日志 |
| LLM 记忆抽取失败率 | ____ | < 5% | > 10% | 应用日志 |
| memory_worker 容器重启次数 | 0 | 0 | > 0 | docker ps |
| memory_worker CPU/内存 | ____ | 稳定 | CPU > 80% 持续 5 分钟 | docker stats |

### 3.3 第一阶段通过标准（满足全部才能进入第二阶段）

- [ ] 记忆写入量相比基线上升至少 3 倍
- [ ] memory_pipeline_task 完成率 > 90%
- [ ] LLM 抽取失败率 < 5%
- [ ] memory_worker 无异常重启
- [ ] 无用户反馈"记忆突然出错"或"AI 说的事不对"
- [ ] 观察期 ≥ 24 小时（建议 48 小时）

### 3.4 第一阶段异常处理

**如果记忆写入量没有上升**：
1. 检查 memory_worker 日志是否有批量处理相关报错
2. 确认 `executeBatchMemoryPipelineTask` 方法是否被调用（日志中搜索 "batch"）
3. 检查 Redis 中批量队列是否正常攒批：`docker exec tzl_redis redis-cli llen "memory-pipeline:batch:*"`
4. 如果确认批量修复未生效，回滚 memory_worker，排查代码问题

**如果 LLM 调用量反而上升**：
1. 检查 `processBatch` 是否被正确调用（应该 1 次批量调用替代 10 次单条）
2. 检查是否批量 LLM 失败后回退到了逐条路径（日志中搜索 "fallback"）
3. 如果回退频繁，检查 LLM 输出格式是否符合预期（带 evidence.messageId）

**如果出现大量记忆错误**：
1. 立即回滚 memory_worker：`bash scripts/rollback-production.sh execute <旧commit> tzl_memory_worker`
2. 检查错误记忆的 sourceMessageId，定位是哪条消息抽取出错
3. 排查 LLM 批量抽取 prompt 是否有问题

---

## 四、第二阶段：发布 tzl_node（用户面）

> 前提：第一阶段已通过，记忆写入稳定至少 24 小时。

### 4.1 操作步骤

```bash
cd /opt/tianzhiling

# 确认当前 tzl_node 还是旧版本（第一阶段回滚的）
docker inspect -f '{{.Config.Image}}' tzl_node

# 将 tzl_node 升级到新版本（镜像在第一阶段已构建好）
docker image tag <新tzl_node镜像ID> tianzhiling-tzl_node:latest
docker compose --profile prod up -d --no-deps --force-recreate tzl_node

# 等待 tzl_node 启动完成（pm2 -i 4，约 30-60 秒）
sleep 60

# 验证健康检查
curl -s https://tianzhiling.chat/api/system/health
# 预期：{"status":"ok",...}

# 验证容器状态
docker ps --format '{{.Names}} {{.Status}}' | grep tzl_node
# 预期：Up，RestartCount 为 0

# 查看启动日志
docker logs tzl_node --tail 50
```

### 4.2 第二阶段观察指标（48-72 小时）

| 指标 | 基线值 | 预期变化 | 告警阈值 | 查看方式 |
|---|---|---|---|---|
| 用户聊天接口成功率 | ____ | 稳定 | < 99% | nginx 日志 / 应用日志 |
| 平均回复延迟 | ____ | 稳定或轻微上升（candidate 注入增加上下文） | > 基线 1.5 倍 | 应用日志 |
| LLM 主模型调用量 | ____ | 稳定 | > 基线 1.2 倍 | LLM 平台账单 |
| LLM 主模型 token 消耗 | ____ | 轻微上升（candidate 事实注入增加 prompt 长度） | > 基线 1.5 倍 | LLM 平台账单 |
| 回复中出现【待确认】记忆被直接断言的比例 | 0（新指标） | 应接近 0 | > 5% | 抽样人工检查 / 审计日志 |
| 用户反馈"AI 记错了"的数量 | ____ | 稳定或下降 | > 基线 2 倍 | 客服反馈 / 用户评价 |
| 用户反馈"AI 突然变笨了"的数量 | ____ | 稳定 | > 基线 2 倍 | 客服反馈 |
| tzl_node 容器重启次数 | 0 | 0 | > 0 | docker ps |
| 证据审计触发率 | ____ | 上升（证据匹配升级后更严格） | — | 应用日志 |

### 4.3 第二阶段重点验证：candidate 弱注入行为

candidate 弱注入是本轮最核心的行为变化，需要重点验证：

**验证 1：candidate 事实是否被注入**

```bash
# 找一个有 candidate 状态事实的用户，发起一轮聊天
# 在应用日志中查看注入的证据包，确认 candidate 事实带【待确认】前缀
# 命令：
docker logs tzl_node 2>&1 | grep -A 5 '待确认' | tail -20
```

**验证 2：主模型是否遵守"不直接断言"约束**

- 人工抽样 20-50 轮有 candidate 事实的对话
- 检查 AI 回复中是否将【待确认】记忆当作已确认事实直接断言（如"我记得你喜欢吃莲蓉馅"而不是"你是不是喜欢吃莲蓉馅来着？"）
- 违规率应 < 5%

**验证 3：candidate 事实是否能被用户确认后升级**

- 找一个 candidate 事实，在聊天中用户明确确认（如"对，我就是喜欢吃莲蓉馅"）
- 检查后续该事实是否升级为 active（status 从 candidate 变为 active）
- 升级路径：supportCount>=2 或 userReconfirmsValue

### 4.4 第二阶段通过标准

- [ ] 聊天接口成功率 > 99.5%
- [ ] 平均回复延迟 < 基线 1.3 倍
- [ ] candidate 事实被直接断言的违规率 < 5%
- [ ] 无大规模用户反馈"AI 记错了"或"AI 变笨了"
- [ ] tzl_node 无异常重启
- [ ] 观察期 ≥ 48 小时（建议 72 小时）

### 4.5 第二阶段异常处理

**如果出现大规模"AI 记错了"反馈**：
1. 立即回滚 tzl_node：`bash scripts/rollback-production.sh execute <旧commit> tzl_node`
2. 排查是 candidate 弱注入导致的，还是证据匹配升级导致的
3. 如果是 candidate 弱注入问题，可以临时通过代码修改将 `listFactsForPrompt` 的 status 过滤改回只返回 active（快速止血）
4. 定位具体问题后修复，重新走灰度流程

**如果回复延迟显著上升**：
1. 检查是否 candidate 事实数量过多导致 prompt 膨胀
2. 检查 LLM 调用是否超时增加
3. 如果是 prompt 膨胀，可以临时限制 candidate 事实注入数量（如最多 5 条）

**如果 LLM token 消耗暴增**：
1. 检查 candidate 事实注入的平均数量
2. 如果注入过多，限制数量或提高 candidate 注入门槛（如只注入 supportCount>=1 的 candidate）
3. 评估是否需要调整 `listFactsForPrompt` 的 limit

---

## 五、第三阶段：发布管理后台和网关

> 前提：第二阶段已通过，用户面稳定至少 48 小时。

### 5.1 操作步骤

```bash
cd /opt/tianzhiling

# 发布 admin_node 和 admin_web（nginx 通常不需要重启，除非 gateway 有变更）
docker compose --profile prod up -d --no-deps --force-recreate tzl_admin_node tzl_admin_web

# 如果 nginx 配置有变更，重新加载
docker exec tzl_nginx nginx -t
docker exec tzl_nginx nginx -s reload

# 验证管理后台
curl -s https://admin.tianzhiling.chat/admin_api/system/health
# 预期：{"status":"ok",...}

# 验证记忆列表新接口
curl -s "https://admin.tianzhiling.chat/admin_api/memories?page=1&pageSize=10"
# 预期：返回记忆事实列表，包含 candidate 状态的记录
```

### 5.2 验证管理后台记忆列表

- [ ] 登录管理后台，进入记忆管理页面
- [ ] 确认能看到所有状态的记忆（active/candidate/conflicted/pending/rejected/archived）
- [ ] 确认能按 status 筛选
- [ ] 确认能按 agentId / userId 筛选
- [ ] 确认列表分页正常
- [ ] 确认每条记录显示 key / value / status / confidence / sourceMessageId / createdAt

---

## 六、全量发布后持续观察（1 周）

全量发布完成后，持续观察以下指标 1 周：

| 时间点 | 检查项 |
|---|---|
| 发布后 24 小时 | 全部服务健康、记忆写入量稳定、无大规模用户反馈 |
| 发布后 48 小时 | candidate 弱注入违规率、LLM 成本变化、用户留存 |
| 发布后 72 小时 | 记忆管道积压情况、Milvus 写入量（如果后续打开检索开关） |
| 发布后 1 周 | 综合评估：记忆质量、用户满意度、成本变化、系统稳定性 |

**1 周后评估是否打开以下开关**：
- `NODE_MILVUS_RETRIEVAL_MODE=active`（让 Milvus 长期记忆可被检索）
- `NODE_CHAT_TOOLS_MODE=active`（让 lookup_chat_evidence 工具真正执行）

> 这两个开关建议先在灰度环境验证，再全量打开。打开后需要额外观察 LLM 工具调用率、检索结果质量、回复延迟变化。

---

## 七、回滚方案

### 7.1 快速回滚（单服务）

```bash
cd /opt/tianzhiling

# 回滚单个服务到指定 commit
bash scripts/rollback-production.sh execute <旧commit> <服务名>

# 示例：回滚 tzl_node
bash scripts/rollback-production.sh execute abc123def456... tzl_node

# 示例：回滚 tzl_memory_worker
bash scripts/rollback-production.sh execute abc123def456... tzl_memory_worker
```

### 7.2 全量回滚

```bash
cd /opt/tianzhiling

# 回滚所有服务到指定 commit
bash scripts/rollback-production.sh execute <旧commit>
```

### 7.3 回滚验证

```bash
# 确认服务已回滚
docker inspect -f '{{.Config.Image}}' <服务名>

# 确认健康检查通过
curl -s https://tianzhiling.chat/api/system/health
curl -s https://admin.tianzhiling.chat/admin_api/system/health

# 确认容器无重启
docker ps --format '{{.Names}} {{.Status}}' | grep tzl_
```

### 7.4 回滚后数据处理

回滚代码后，已写入的记忆数据不会自动回滚。如果新代码写入了错误数据：

1. 定位错误数据的时间范围（发布时间到回滚时间）
2. 在 MongoDB 中标记或删除该时间段写入的错误事实
3. 检查 `memory_pipeline_task` 中该时间段的任务，必要时重新处理
4. 通知受影响的用户（如果涉及用户可感知的错误）

> 注意：删除记忆数据是高风险操作，建议先标记为 archived 而不是物理删除，观察一段时间后再清理。

---

## 八、常用命令速查

### 8.1 服务管理

```bash
# 查看所有服务状态
docker ps --format '{{.Names}}\t{{.Status}}\t{{.Ports}}' | grep tzl_

# 查看单个服务日志
docker logs tzl_memory_worker --tail 100 -f
docker logs tzl_node --tail 100 -f

# 重启单个服务
docker compose --profile prod restart tzl_memory_worker

# 查看资源占用
docker stats --no-stream
```

### 8.2 发布相关

```bash
cd /opt/tianzhiling

# 查看发布进度
bash scripts/release-production.sh progress

# 预览发布范围（不执行）
bash scripts/release-production.sh plan <branch> <commit>

# 执行发布
bash scripts/release-production.sh <branch> <commit>

# 预览回滚范围
bash scripts/rollback-production.sh plan <commit>

# 执行回滚
bash scripts/rollback-production.sh execute <commit> [服务名...]
```

### 8.3 MongoDB 查询

```bash
# 连接 MongoDB
docker exec -it tzl_mongo mongosh

# 查看记忆事实状态分布
use tzl
db.agent_profile_fact.aggregate([{$group: {_id: "$status", count: {$sum: 1}}}])

# 查看最近 1 小时新增记忆
db.agent_profile_fact.find({createdAt: {$gte: new Date(Date.now() - 3600000)}}).count()

# 查看记忆管道任务状态
db.memory_pipeline_task.aggregate([{$group: {_id: "$status", count: {$sum: 1}}}])

# 查看最老的 pending 任务年龄
db.memory_pipeline_task.find({status: "pending"}).sort({createdAt: 1}).limit(1)
```

### 8.4 Redis 查询

```bash
# 连接 Redis
docker exec -it tzl_redis redis-cli

# 查看批量队列长度
KEYS memory-pipeline:batch:*

# 查看某个队列的消息 ID
LRANGE memory-pipeline:batch:<conversationId> 0 -1
```

### 8.5 健康检查

```bash
# 用户面健康
curl -s https://tianzhiling.chat/api/system/health | jq

# 管理后台健康
curl -s https://admin.tianzhiling.chat/admin_api/system/health | jq

# Milvus 健康
curl -s http://127.0.0.1:19091/healthz

# 记忆列表接口（需 admin 认证）
curl -s "https://admin.tianzhiling.chat/admin_api/memories?page=1&pageSize=5" | jq
```

---

## 九、发布记录

| 阶段 | 计划时间 | 实际时间 | 执行人 | 结果 | 备注 |
|---|---|---|---|---|---|
| 第一阶段 memory_worker | | | | ⬜ 待发布 | |
| 第一阶段观察期 | | | | ⬜ 观察中 | |
| 第一阶段通过/回滚 | | | | ⬜ | |
| 第二阶段 tzl_node | | | | ⬜ 待发布 | |
| 第二阶段观察期 | | | | ⬜ 观察中 | |
| 第二阶段通过/回滚 | | | | ⬜ | |
| 第三阶段 admin/nginx | | | | ⬜ 待发布 | |
| 全量稳定观察 1 周 | | | | ⬜ | |
| 评估打开检索开关 | | | | ⬜ | |

---

*文档版本：v1.0 | 生成时间：2026-09-10 | 基于天之灵记忆系统第一轮+第二轮修复代码*
