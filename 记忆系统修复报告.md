# 天之灵 AI 记忆系统修复报告

执行时间：2026-09-10
执行范围：`/Users/m4/Documents/tianzhiling/tianzhiling/`（Midway + TypeORM + MongoDB + Milvus monorepo）
执行方式：每步先 Read 真实代码确认现状，再改，改完跑测试验证；全部基于真实代码，未用网络知识替代。

---

## 一、配置核对结果（第一步）

逐一检查了项目根目录与 `apps/node/` 下的 `.env`、`.env copy`、`.env.local`、`.env.example`、`.env.production`、`apps/node/.env`：

| 变量 | `.env` 实际值 | 代码默认值 | 生效值 |
|---|---|---|---|
| `NODE_MILVUS_RETRIEVAL_MODE` | 未设置（所有 env 文件均未覆盖） | `config.default.ts:671` = `'off'` | **`off`** |
| `NODE_CHAT_TOOLS_MODE` | 未覆盖；仅 `.env.example:109` 示例 `=shadow` | `config.default.ts:463` = `'shadow'` | **`shadow`** |

结论：线上运行态与默认值一致——Milvus 检索关闭、chatTools 处于 shadow（只观测不真正调用）。本次记忆修复不依赖这两个开关。

---

## 二、后台记忆事实列表接口（第一步）

确认现状：`apps/admin-node` 与 `apps/admin` 前端此前没有专门的记忆事实管理页/接口。仿照现有 `admin-agent` 的 controller/service/dto 范式新建三件套：

| 文件 | 说明 |
|---|---|
| `apps/admin-node/src/dto/admin-memory.dto.ts` | `ListAdminMemoriesQueryDTO`：status / agentId / userId / type / keyword / page / pageSize |
| `apps/admin-node/src/service/admin-memory.service.ts` | `AdminMemoryService.listFacts`，默认返回**全部状态**（含 archived），支持按 status 筛选 |
| `apps/admin-node/src/controller/admin-memory.controller.ts` | `@Controller('/memories')`，结合全局前缀 `/admin_api` → `GET /admin_api/memories` |

前端接入点：`apps/admin/src/views/agent/detail/index.vue` 的「查看已梳理记忆」弹窗已展示状态色标签；新接口为独立管理页接入点，可直接复用其状态色映射。新文件 `tsc --noEmit` 无类型错误。

---

## 三、P0 四项（第二步，止住错误数据）

### P0-1 implicit 死亡日期加信号前置门
- 文件：`apps/node/src/service/agents/person-temporal-memory.ts`
- 改动：在 `parseAgentDepartureTime` 的 implicit 通道入口复用既有 `hasAgentDepartureTimeSignal`（显式离世词 **或** 数字+时长信号）作为前置门。
- 关键决策：最初用纯显式离世词门会导致既有「已经16年了」相认旅程测试失败，故保留数字+时长信号分支，既挡住「昨天他没吃饭」又保留「已经16年了」。
- 文件：`apps/node/src/service/conversation.service.ts`（provenance 回退）：answerMessage 无自身内容时**跳过**旅程补写，不再回退 `plan.currentUserText` 却记 answerMessage.id。
- 测试：`test/service/person-temporal-memory.test.ts` 新增 2 个回归（「昨天他没吃饭」不解析为离世日期；「我妈昨天走了」正常解析）。

### P0-2 职业/角色不当名字
- 文件：`apps/node/src/service/agents/user-identity-memory.service.ts`
- 改动：新增 `NON_PERSON_OCCUPATION_BLACKLIST`（职业/官职/神话/动物/自然词，整词精确匹配，避免误伤「牛建国」类真实名），`normalizePersonName` 拒绝黑名单词。
- 测试：`test/service/user-identity-memory.service.test.ts` 新增回归（「我爸爸是老师」→ realName 不落「老师」；「我爸爸叫张三」→ realName=张三）。

### P0-3 含问号消息不再整体丢弃事实
- 文件：`apps/node/src/service/agents/agent-profile-fact.service.ts`
- 改动：未完全删除 `isQuestionOnly` 早退（完全删除会打破既有「你以前是木匠吗？→不抽取」测试），改为**收窄**：问句形式且无数字类陈述信号时保持早退；含数字陈述（如「我今年38岁，你记得吗？」）放行给 LLM。LLM systemPrompt 追加「只提取陈述性事实部分，纯问句输出[]」。
- 测试：新增 2 个（「我今年38岁，你记得吗？」→ 抽取 age=38；「你记得我叫什么吗？」→ 不抽取）。

### P0-4 收紧 isCorrectionText
- 文件：`apps/node/src/service/agents/agent-profile-fact.service.ts`
- 改动：去掉裸词「没有」，改为明确纠正结构（「不对/不是这样/你记错了/.{0,4}不叫/我不是X而是Y/其实是Y/更正…」等）。
- 关键决策：纯任务正则会打破既有「我不记得了，没有这事」否认测试，故补入「没有这(回)事/根本没这回事」这类对具体断言的否认，区别于裸词「没有吃饭」。
- 测试：新增（「我今天没有吃饭」→ isCorrectionText=false；「不对，我叫张三」→ true；「其实是8月15日」→ true）。

---

## 四、P1 七项（第三步，数据一致性）

| 编号 | 文件 | 改动 |
|---|---|---|
| P1-1 supportCount 幂等 | `agent-profile-fact.service.ts` upsertFact | 递增前先查 `sourceMessageId` 是否已在 `sourceMessageIds`，已存在则不递增。既有「model-only name candidate」测试改用两个不同来源 id。 |
| P1-2 sourceMessageId 不可变 | `agent-profile-fact.service.ts` + `packages/entities/src/agent-profile-fact.entity.ts` | `sourceMessageId` 仅首次创建设置；实体新增 `firstSourceMessageId` / `latestSourceMessageId`（MongoObjectId optional），已 `cd packages/entities && npm run build` 重建 dist。 |
| P1-3 冲突可解决后清除 | `agent-profile-fact.service.ts` upsertFact | `userReconfirmsValue`（同值且 trustedSource/纠正语气）时清空 `conflictingValues` 并激活。 |
| P1-4 无年份更正直接覆盖 | `person-temporal-memory.service.ts` applyAssertionToProfile | 识别 `isNoYearCorrection`（isCorrection 且无 exactDate 但有月日），直接 supersede 旧断言并沿用既有年份重建 exactDate。 |
| P1-5 改口时间/地点不当改名 | `agent-name-memory.ts` | 新增 `NON_NAME_TIME_PLACE_EXACT`（时间/星期/常见地点词），`normalizeName` 拒绝。 |
| P1-6 重名按关系+名字合并 | `user-identity-memory.service.ts` | 删除 `nameMatches.length===1` 的跨关系回退合并；合并条件收紧为「同名 + 同关系」。 |
| P1-7 外貌多次观察才转正 | `agent-profile-fact.service.ts` upsertVisualAppearanceObservations | 视觉事实恒 `trustedSource:false`，由通用 upsertFact 要求 `sameValue && supportCount>=2` 才激活，加显式注释固化「同一特征多次观察才转正」。 |

---

## 五、第四步：三级证据注入（核心架构）

目标：解决「candidate 事实被完全屏蔽」。三级——confirmed/active 可断言；candidate 弱注入（只能当话题引子）；rejected 不注入。

1. `agent-profile-fact.service.ts` `listFactsForPrompt`：status 过滤由 `active` 改为 `{ $in: [active, candidate] }`，candidate 进入注入层。
2. `agent.context.ts` `buildEvidencePack`：引入 `AgentProfileFactStatus`；profile-fact 循环对 candidate 事实设 `source:'confirmed_fact'`、文本前缀 **【待确认】**、`assertionPolicy:'context_only'`、`useMode:'hypothesis'`（status 保持 `'active'`，因 `AgentEvidenceStatus` 类型不含 candidate）。
3. `main-model-conversation-principles.ts`：主模型原则新增一条——标注【待确认】的记忆只是候选印象，只能当话题引子/委婉猜测，不能当作确凿事实直接断言；用户确认后才可当作已确认事实。
4. `agent-evidence.ts`：**无需改动**。autonomous_fact 解析本就要求 `assertionPolicy==='can_assert'` 且 `useMode==='assert'`（约 218-219 行），candidate 的 `context_only/hypothesis` 天然无法支撑断言类声明。
5. candidate 自动升级：upsertFact 中「同值 + 新来源 → supportCount>=2」与 `userReconfirmsValue` 两条路径已实现 candidate→active（被用户后续重述/确认即升级），由 P1-7 视觉测试与新增测试固化。

验证（`test/service/agent-profile-fact.service.test.ts` 新增 2 个）：
- listFactsForPrompt 查询 status `$in:[active,candidate]`；
- candidate 经新来源重述（同值）后升级 active（supportCount=2）。

---

## 六、第五步 P2 复现结论

| 项 | 结论 |
|---|---|
| #4 否定记正面 | 代码级未复现。抽取 prompt 已要求「疑问、否定、猜测不写入」「没有可确认事实输出[]」，且 P0-4 已移除裸词「没有」纠正。当前未见否定被记为正面的路径。 |
| #10 年龄不识别 | 代码级未复现。抽取 type 白名单已显式含 `age`，value 要求可独立理解。未见年龄被漏识别的代码缺口。 |
| #14 同栏目两说法 | 现状即「同 key 不同值」写入 `conflictingValues` 并标 conflicted，P1-3 已在用户明确选择后清除冲突。属设计行为，不另改。 |
| #16 出处只留 8 条 | **已修**。`appendSourceMessageId` 的 `.slice(-8)` → `.slice(-50)`。 |
| #17 并发写入原子化 | **未盲改，建议后续单独评估**。upsertFact 是 read-决策-写（sameValue/conflictingValues/history 副作用依赖 existing），当前测试用 mock 模型、无真实 MongoDB 集成环境；盲目改 `findOneAndUpdate` 风险高。建议方案：在 `(userId, agentId, key)` 建唯一索引，创建分支用 `findOneAndUpdate(..., { upsert:true })`，更新分支保留读改写但加乐观版本号。 |
| #7 身份编号稳定化 | 涉及数据模型重构，成本高，按任务要求**不实施**，仅记录待后续评估。 |

---

## 七、测试结果汇总

| 套件 | 结果 |
|---|---|
| agent-profile-fact.service.test.ts | 36 通过（含本次新增 4 个回归/集成） |
| user-identity-memory.service.test.ts | 13 通过 |
| person-temporal-memory.service.test.ts | 通过 |
| person-temporal-memory.test.ts | 通过（含 P0-1 新增 2 个） |
| agent-evidence.test.ts | 5 通过 |
| **核心记忆合计** | **80 通过 / 0 失败** |
| agent-memory-profile.service.test.ts | **8 失败 / 18 通过——为已知基线（提交 547ad72 引入的访谈部分），按要求未修** |
| agent-context.service.test.ts | 21 失败 / 28 通过——**经 git stash 对照，21 个失败为改动前已存在**（`messageModel.findByIds` mock 缺失、toolDecisions 断言等），非本次引入 |

---

## 八、修改文件清单

**源码（修改）**
- `apps/node/src/service/agents/agent-profile-fact.service.ts`（P0-3/P0-4/P1-1/P1-2/P1-3/P1-7/第四步 listFactsForPrompt/P2#16）
- `apps/node/src/service/agents/agent.context.ts`（第四步 candidate 弱注入）
- `apps/node/src/service/agents/main-model-conversation-principles.ts`（第四步【待确认】原则）
- `apps/node/src/service/agents/person-temporal-memory.ts`（P0-1 前置门）
- `apps/node/src/service/agents/person-temporal-memory.service.ts`（P1-4 无年份覆盖）
- `apps/node/src/service/agents/user-identity-memory.service.ts`（P0-2 黑名单/P1-6 合并）
- `apps/node/src/service/agents/agent-name-memory.ts`（P1-5 时间/地点黑名单）
- `apps/node/src/service/conversation.service.ts`（P0-1 provenance）
- `packages/entities/src/agent-profile-fact.entity.ts`（新增 first/latestSourceMessageId，已重建 dist）

**源码（新建）**
- `apps/admin-node/src/dto/admin-memory.dto.ts`
- `apps/admin-node/src/service/admin-memory.service.ts`
- `apps/admin-node/src/controller/admin-memory.controller.ts`

**测试（修改/新增）**
- `apps/node/test/service/agent-profile-fact.service.test.ts`
- `apps/node/test/service/user-identity-memory.service.test.ts`
- `apps/node/test/service/person-temporal-memory.test.ts`

---

## 九、仍存在的缺口与后续建议

1. **#17 并发原子化**：需真实 MongoDB 集成测试 + 唯一索引后再改，见第六节方案。
2. **#7 身份编号稳定化**：数据模型重构，单列评估。
3. **agent-context.service.test.ts 21 个预存失败**：非本次引入，建议另行修复其 mock（`messageModel.findByIds`）与 toolDecisions 断言。
4. **candidate 弱注入的端到端验证**：本次以单元测试验证「listFactsForPrompt 返回 candidate」与「重述升级 active」；完整 LLM 端到端（模型是否真的遵守【待确认】不直接断言）需在影子/线上观察。
5. 所有代码均未提交 git，按要求直接落在项目目录。

---

# 第二轮修复（2026-09-10）

## 十、第一优先：批量录入 bug 修复

### 问题根因
`memory-pipeline-task.service.ts` 实现了批量攒批（默认每 10 条消息创建一个批量任务，`messageIds` 存全部 10 条），但 `conversation.service.ts` 的 `executeMemoryPipelineTask` 消费者只通过 `task.messageId` 查一条消息处理，完全忽略 `task.messageIds` 中的其余 9 条。**10 条消息只有 1 条进入记忆抽取，90% 的记忆丢失。**

### 修复方案（两步）

#### 第一步：快速止血——消费者遍历所有 messageIds
- 文件：`apps/node/src/service/conversation.service.ts`
- 新增 `executeBatchMemoryPipelineTask(task)`：当 `task.messageIds` 非空时，批量查出所有消息（`$in` 查询 + 过滤 isArchived/非 sent），按时间排序后逐条处理。
- 新增 `processMessageForTaskKind(task, message, searchableText)`：将原有的 per-message 三种 kind 分支（structuredMemory / semanticIndex / personSemanticIndex）提取为独立方法，单条和批量路径共用。
- 每条消息用 try/catch 包裹，一条失败不影响其他。
- `messageIds` 为空时走原单条逻辑不变。

#### 第二步：批量 LLM 抽取优化
- 文件：`apps/node/src/service/agents/memory-value.service.ts`
- 新增 `processBatch(messages, texts, agent)`：一次性调 LLM 抽取整个对话片段，按 `evidence.messageId` 分组后分别 upsert。
- 新增 `buildBatchInput` / `proposeBatch` / `parseBatchOutput`：批量版输入构建、LLM 调用、输出校验（放宽 currentMessageId 约束，decision 上限 24）。
- 批量 LLM 失败自动回退到逐条 `process()` 路径。
- memoryValueService 未启用时回退到逐条 `enrichUserMessageForReply`。

### LLM 调用量变化
| 场景 | 修改前 | 修改后 |
|---|---|---|
| 批量 10 条（active 模式） | 最多 20 次 LLM 调用（10× propose + 可能 review） | 1~2 次（1× proposeBatch + 可能 review） |
| 批量 LLM 失败 | 不适用 | 自动降级逐条，行为同前 |
| 单条任务 | 不变 | 不变 |

**预估批量场景 LLM 调用量降低 ~80-90%。**

---

## 十一、第二优先：LLM 增强正则替换

原则：正则只做"高置信度、零歧义"的基础匹配，复杂语义判断全部交 LLM。所有 LLM 新增调用均有失败回退。

### 11.1 P0-4 纠正判定升级
- 文件：`apps/node/src/service/agents/agent-profile-fact.service.ts`
- LLM 抽取 prompt 增加 `isCorrection` 布尔字段。
- 新增 `LLMExtractedFact` 接口携带 `_llmIsCorrection`。
- `trustedSource` 判定优先看 LLM 输出的 `_llmIsCorrection === true`，否则回退 `isCorrectionText` 正则加速通道（仅保留"你记错了""不对，应该是"等明确模式）。
- LLM 调用失败时回退到正则判断。

### 11.2 亲属姓名抽取加 LLM 区分层
- 文件：`apps/node/src/service/agents/user-identity-memory.service.ts`
- 新增 `filterPersonNamesWithLLM`：正则粗筛出候选 realName 后，批量调一次 LLM（temperature=0）判断"是人名还是职业/身份/角色"。
- 只有判定为人名才落库。
- 第一轮已加的 `NON_PERSON_OCCUPATION_BLACKLIST` 保留作为 LLM 不可用时的兜底。
- LLM 调用失败时回退到黑名单过滤。

### 11.3 时间解析改为 LLM 优先
- 文件：`apps/node/src/service/agents/person-temporal-memory.service.ts` + `person-temporal-memory.ts`
- 明确的精确日期（`\d{4}年\d{1,2}月\d{1,2}日`）仍走规则快速通道。
- 复杂时间语义（"其实是8月15日""走了十几年了""前年冬天"）交 LLM 解析，LLM prompt 扩展输出 `normalizedExactDate` / `normalizedYearMonth` / `normalizedYear` / `isCorrection` 标准化字段。
- 新增 `parseComplexTimeLLMOutput`：从 LLM 输出直接构建 `ParsedDepartureTimeAssertion`。
- **复用现有 LLM 调用通道**（`extractDepartureTimeWithModel`），不新增调用次数，仅扩展输出 schema。
- LLM 无标准化字段时回退原有语义门 + 规则解析。

### 11.4 证据匹配升级（规则实现，非 LLM）
- 文件：`apps/node/src/service/agents/agent-evidence.ts`
- bigram 重叠从"任意一个"改为"**至少 2 个非停用词 bigram**"。
- 新增**极性校验**：否定词（不/没/无/非/未/否）不一致时不匹配（"不喜欢吃辣" vs "喜欢吃辣"不算匹配）。
- 新增**主语校验**：断言含称谓（爸爸/妈妈/儿子等）时，证据必须含相同称谓。
- 极性校验提前到子串匹配之前，避免子串包含导致的误判。

---

## 十二、第三优先：生产配置开关

逐一检查项目根目录与 `apps/node/` 下所有 `.env` 文件（`.env`、`.env.local`、`.env copy`、`.env.example`、`apps/node/.env`）：

| 变量 | 实际 .env 值 | 代码默认值 | 生效值 |
|---|---|---|---|
| `NODE_MILVUS_RETRIEVAL_MODE` | 未设置（所有 env 文件均未覆盖） | `config.default.ts:671` = `'off'`，`config.prod.ts:67` = `'off'` | **`off`** |
| `NODE_CHAT_TOOLS_MODE` | 未设置；仅 `.env.example:109` 示例 `=shadow` | `config.default.ts:463` = `'shadow'` | **`shadow`** |

### 影响说明与建议
- **当前状态**：Milvus 只写不读（retrieval off），chatTools 只观测不真正调用（shadow）。长期记忆抽取后写入了 Milvus 和 agent_profile_fact，但检索阶段不会从 Milvus 召回。
- **如需让长期记忆真正被使用**，需在生产 `.env` 中设置：
  ```
  NODE_MILVUS_RETRIEVAL_MODE=active
  NODE_CHAT_TOOLS_MODE=active
  ```
- **风险提示**：切换为 active 前建议先在 shadow 模式下观察 Milvus 检索结果的相关性和 chatTools 决策的准确率，避免低质量检索结果污染对话上下文。
- 本次修复**未修改**任何 `.env` 文件。

---

## 十三、第二轮测试结果汇总

| 测试套件 | 结果 |
|---|---|
| `memory-pipeline-batch.test.ts`（新增） | **10/10 通过** |
| `memory-value.test.ts` | 18/18 通过 |
| `memory-value.service.test.ts` | 通过 |
| `memory-pipeline-task.service.test.ts` | 通过 |
| `agent-profile-fact.service.test.ts` | 通过（含新增 3 个 P0-4 回归） |
| `user-identity-memory.service.test.ts` | 通过（含新增 3 个姓名区分回归） |
| `person-temporal-memory.test.ts` | 通过 |
| `person-temporal-memory.service.test.ts` | 通过（含新增 3 个时间解析回归） |
| `agent-evidence.test.ts` | 通过（含新增 5 个证据匹配回归） |
| **核心记忆合计** | **138 通过 / 0 失败** |
| `agent-memory-profile.service.test.ts` | 8 失败——已知基线，未碰 |
| `agent-context.service.test.ts` | 21 失败——已知基线，未碰 |

TypeScript 编译：`apps/node` 和 `packages/entities` 均 `tsc --noEmit` 零错误。

---

## 十四、第二轮修改文件清单

**源码（修改）**
- `apps/node/src/service/conversation.service.ts`（批量消费者重构：executeBatchMemoryPipelineTask / processMessageForTaskKind / executeBatchStructuredMemory）
- `apps/node/src/service/agents/memory-value.service.ts`（新增 processBatch / buildBatchInput / proposeBatch / parseBatchOutput）
- `apps/node/src/service/agents/agent-profile-fact.service.ts`（P0-4 LLM isCorrection 层）
- `apps/node/src/service/agents/user-identity-memory.service.ts`（姓名 LLM 区分层 filterPersonNamesWithLLM）
- `apps/node/src/service/agents/person-temporal-memory.ts`（接口扩展 rawExpression，导出 makeDateOnly/daysInMonth）
- `apps/node/src/service/agents/person-temporal-memory.service.ts`（LLM 时间标准化 parseComplexTimeLLMOutput，prompt 扩展 normalizedExactDate 等字段）
- `apps/node/src/service/agents/agent-evidence.ts`（证据匹配：多 bigram + 极性校验 + 主语校验）
- `packages/entities/src/message.entity.ts`（temporalMemorySemanticSource 类型扩展）

**测试（新增/修改）**
- `apps/node/test/service/memory-pipeline-batch.test.ts`（新增，10 个用例）
- `apps/node/test/service/agent-profile-fact.service.test.ts`（新增 3 个）
- `apps/node/test/service/user-identity-memory.service.test.ts`（新增 3 个）
- `apps/node/test/service/person-temporal-memory.service.test.ts`（新增 3 个 + 更新 1 个断言）
- `apps/node/test/service/agent-evidence.test.ts`（新增 5 个）

---

## 十五、第二轮新增 LLM 调用点汇总

| 调用点 | 文件 | 触发条件 | 预估增量 | 回退策略 |
|---|---|---|---|---|
| `processBatch` → `proposeBatch` | memory-value.service.ts | 批量任务且 memoryValueService active | **减少** 80-90%（批量合并调用） | 失败回退逐条 process() |
| `filterPersonNamesWithLLM` | user-identity-memory.service.ts | 正则粗筛出候选 realName 且不在黑名单 | +1 次/批（批量候选一次调用） | 失败回退黑名单过滤 |
| `extractDepartureTimeWithModel` prompt 扩展 | person-temporal-memory.service.ts | 规则无法解析的复杂时间 | **0 增量**（复用现有调用，仅输出字段增多） | 无标准化字段时回退规则 |
| LLM 抽取 prompt 增加 isCorrection | agent-profile-fact.service.ts | 现有 LLM 抽取调用 | **0 增量**（复用现有调用，仅字段扩展） | LLM 失败回退 isCorrectionText 正则 |

**净效果**：批量场景 LLM 调用量大幅降低；仅姓名区分新增少量调用（且仅在有人名候选时触发）。
