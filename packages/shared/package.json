{
  "name": "@tzl/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "clean": "rm -rf dist",
    "sync:china-regions": "node scripts/sync-china-regions.mjs"
  },
  "devDependencies": {
    "@types/node": "14",
    "typescript": "~4.8.0"
  }
}
