# 天之灵发布记录

本文件记录天之灵每次发布的状态双写（与 `release-state/*.json` 一一对应）。

---

## 20260830 — 管理后台收入统计与仪表盘修复

- **分支**：20260830
- **基线提交**：`59e813c`（admin income stats + dashboard fix 20260830）
- **日期**：2026-08-30
- **状态**：待部署
- **上游基线**：20260827

### 包含变更

- **当天收入统计包含会员降级差价退款**：`buildRefundFlowMatch` 增加 `status=completed 且 refundAmount>0` 的部分退款匹配，降级退款不再漏算。
- **降级退款记录退款时间**：`completeVoiceMembershipDowngrade` 成功时设置 `order.refundedAt`，确保按退款时间归集。
- **净收入口径单元测试**：验证降级差价只扣一次，不与 snapshot 重复扣减。
- **数据仪表盘图表修复**：Chart 组件 props 名 `options` → `option`，修复图表不渲染。
- **数据仪表盘布局修复**：grid span 响应式对象改为固定值 16/8，修复图表宽度为0。

### 数据修复（已在生产库执行）

- 6笔历史降级退款从微信支付获取实际 `success_time`，补写 `order.refundedAt`（8/7、8/19、8/22、8/23、8/24、8/28）。
- `admin_account` 重复记录已清理。

### 双写产物

- `release-state/20260830.json` + 本文档。

---

## 20260827 — 生产基线 3017ee4

- **分支**：20260827
- **基线提交**：`3017ee4`（prod baseline 20260827）
- **日期**：2026-08-27
- **状态**：已部署

### 包含变更

- **milvus OOM 加固**：熔断 / 超时 / 并发上限。
- **撤回次日长文**：恢复当轮直接回复（次日长文降级为休眠代码）。
- **管理端运营看板 + Doubao ICL 2.0**：发布。
- **持续倾听收缩**：改为短时聚合。
- **聊天质量更新 + 生产堆内存故障修复**。
- **admin 证书续期**。

### 下游

- 未了言 20260830 对齐发布以本基线为上游。

### 双写产物

- `release-state/20260827.json` + 本文档。
